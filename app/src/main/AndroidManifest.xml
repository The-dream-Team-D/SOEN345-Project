<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.PopIn">
        <activity
            android:name=".uipages.MainActivity"
            android:theme="@style/Theme.PopIn.Starting"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".uipages.EventsPageActivity" />
        <activity android:name=".uipages.ForgotPasswordActivity" />
        <activity android:name=".uipages.LogInActivity" />
        <activity android:name=".uipages.SignUpActivity" />
        <activity android:name=".uipages.MyTicketsActivity" />
        <activity android:name=".uipages.ProfileActivity" />
        <activity android:name=".uipages.AdminDashboardActivity" />
        <activity android:name=".uipages.EventDetailActivity" />
        
    </application>

</manifest>
